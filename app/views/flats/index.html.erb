<div class="map">
  <h4>Adventures around the globe await!</h4>
  <%= render 'shared/map' %>
</div>
<!-- <div
id="map"
style="width: 600px;
height: 400px;"
data-markers="<%= @markers.to_json %>">
</div> -->

<div class="banner-flats-index">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4 ">
      <div class="search-form text-align-left">
        <%= form_tag search_path, method: "get", :class => 'comment_form' do %>
          <br>
          <h1>Want to rent a place? Book now!</h1>
          <br>
          <p>
            <%= label_tag(:city, "WHERE:") %>
            <%= text_field_tag(:city, "Anywhere")%>
          </p>
          <div class="date_search_form">
            <p>
              <%= label_tag(:q, "CHECK IN:") %>
              <%= date_field(:booking, :start_date) %>
            </p>
            <p><%= label_tag(:q, "CHECK OUT:") %>
            <%= date_field(:booking, :end_date) %></p>
          </div>
          <p>
            <%= label_tag(:q, "GUESTS") %>
            <%= number_field(:flat, :guest_capacity, in: 1..20, step: 1) %>
          </p>
          <div class="btn"><%= submit_tag("Search") %>
          </div>
          <br>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <% @flats.each do |flat| %>
      <div class="col-xs-12 col-sm-6 col-md-3">
        <div class="card-index">
          <%= link_to flat_path(flat) do %>
            <div class="card-body-index">
              <%= cl_image_tag flat.photo, height: 300, width: 400, crop: :fill %>
            </div>
            <div class="card-footer-index">
              <h1><%= flat.description %></h1>
              <p><strong>Price: </strong><%= flat.price %>CAD</p>
              <p><strong>Ratings :</strong> <%= flat.average_rating%></p>
              <p><strong>Reviews:</strong> <%= flat.number_of_review%></p>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>
